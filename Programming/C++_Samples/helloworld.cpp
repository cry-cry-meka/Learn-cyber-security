#include <iostream> // Includes the iostream library for input and output operations

int main() // Main function where the program starts execution
{
    std::cout << "Hello World!"; // Outputs "Hello World!" to the console
    std::cin.ignore();           // Waits for the user to press Enter before closing (ignores one character from the input buffer)

    return 0; // Returns 0 to indicate successful program execution
}
